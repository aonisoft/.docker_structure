
<VirtualHost *:80>
    ServerName ${URL_FRONTEND}

    ServerAlias ${URL_FRONTEND_ALIAS}
    
    ProxyPreserveHost On
    
    ProxyPass / "http://${NODE_CONTAINER_NAME}:3032/" keepalive=On
    ProxyPassReverse / "http://${NODE_CONTAINER_NAME}:3032/"

</VirtualHost>