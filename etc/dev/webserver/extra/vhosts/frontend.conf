
<VirtualHost *:8081>   
    ProxyPreserveHost On
    
    ProxyPass / "http://${FRONTEND_CONTAINER_NAME}:${PORT_FRONT}/" keepalive=On
    ProxyPassReverse / "http://${FRONTEND_CONTAINER_NAME}:${PORT_FRONT}/"

</VirtualHost>